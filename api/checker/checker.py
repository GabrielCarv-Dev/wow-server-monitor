# Carregar sessão (SessionLocal)

# Buscar targets enabled=True

# Para cada target:

# medir tempo (time.monotonic() antes/depois)

# fazer requests.get() num endpoint “simulado” (por hora)

# gravar Check(status, latency_ms, error)

# time.sleep(60) (ou manual)